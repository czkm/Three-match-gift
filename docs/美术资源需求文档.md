# 白鸦葡萄园动态场景美术资源需求文档

## 1. 结论

这个需求可行。当前工程已经有棋盘下方的庄园展示位，后续无论继续沿用现有结构，还是升级为 `Canvas 2D` 场景系统，核心都不在玩法逻辑，而在**美术资源是否足够分层、可切换、可循环**。

你需要准备的，不是单张大背景，而是：

- 一套可拼装的场景底图
- 9 天阶段差分资源
- 可点击角色/动物素材
- 粒子与环境氛围素材
- 对应的分层源文件与导出规范

同时，项目可以先采用**占位符方案**推进：

- 先用 `emoji + CSS + 简单色块/渐变` 完成状态切换、交互热点、阶段演出
- 后续再把占位符逐步替换成正式美术资源
- 只要前期把命名、锚点、分层关系定好，后期替换不需要重写玩法逻辑

## 2. 目标规格

- 展示区参考尺寸：`680×160`
- 建议出图规格：`1360×320`，按 2x 交付，便于像素风或低清风格缩放
- 画面风格：**巫师 3 气质 + 原创构图 + 像素化表现**
- 视角要求：横向全景，庭院到主宅连贯，能容纳 9 天阶段切换
- 资源要求：透明背景、可分层、可循环、可点击热点可定位

## 3. 占位符优先开发方案

如果美术暂时不到位，建议按下面顺序推进：

### 3.1 第一阶段：占位符先跑通

这一阶段不等正式美术，先把功能做完整：

- 基础场景先用纯色块、渐变、简化线条表示天空、地面、建筑轮廓
- 角色先用 `emoji` 或文字图标占位
- 粒子先用 CSS 圆点、光斑、半透明块、简单字符表示
- 点击交互、文案反馈、阶段切换、动画节奏先全部接通

这一阶段的目标不是“好看”，而是确保：

- 9 天状态切换逻辑完整
- 角色点击热点位置合理
- 粒子/角色/场景层级不打架
- 后续资源替换时不改数据结构

### 3.2 第二阶段：局部替换正式资源

正式美术可以按优先级逐步替换，不需要一次到位：

- 先替换基础场景底图
- 再替换 9 天差分建筑层
- 再替换角色/动物
- 最后替换粒子、滤镜、结局特效

### 3.3 占位符与正式资源的对应关系

占位符阶段就要提前约定好资源位，避免后期返工：

| 模块 | 占位符形式 | 后续替换形式 |
| --- | --- | --- |
| 白鸦 | `🕊` / `🐦` | 独立角色帧动画或 sprite sheet |
| 萝卜 | `🐎` | 独立角色帧动画或 sprite sheet |
| 蝴蝶 | `🦋` | 独立角色帧动画 |
| 蜜蜂 | `🐝` | 小体积循环飞行动画 |
| 杰洛特剪影 | 深色块剪影 | 窗内人物剪影帧动画 |
| 花瓣/光点 | 圆点、字符、半透明块 | 粒子 PNG / sprite sheet |
| 温室灯、酒窖微光 | 半透明发光层 | 发光贴图或滤镜层 |
| 炊烟 | CSS 渐隐气泡/烟雾块 | 烟雾循环帧动画 |

## 4. 必交资源

### 4.1 基础场景

需要一套可拆分的基础场景层：

- 天空层
- 远景层
- 中景层
- 前景层
- 地面/路径层
- 遮罩/滤镜层

### 4.2 9 天阶段差分

每一天都要能在同一张场景里看到“修复完成后的变化”，建议按“基础场景 + 差分层”方式交付：

| 天数 | 需要表现的变化 |
| --- | --- |
| Day 1 | 庭院门柱、石路、喷泉、主宅外墙、白鸦 |
| Day 2 | 葡萄架、葡萄藤嫩芽、远景葡萄园 |
| Day 3 | 酒窖拱门、木桶、酒瓶、酒窖微光 |
| Day 4 | 马厩、窗口、萝卜头、干草与水槽 |
| Day 5 | 花园、花坛、紫丁香花苞、蝴蝶 |
| Day 6 | 温室、玻璃屋顶、盆栽嫩芽、灯光 |
| Day 7 | 露台、栏杆、圆桌、单把椅子、白鸦 |
| Day 8 | 厨房窗户、炊烟、蜂箱、杰洛特剪影 |
| Day 9 | 紫丁香客房、窗帘、花瓶、两把椅子、最终定格 |

### 4.3 角色/动物

需要独立可拆的角色素材：

- 白鸦：停驻、扇翅、起飞、盘旋、落肩、最终静止
- 萝卜：探头、打响鼻、缩回、再次探头
- 蝴蝶：停落、飞起、盘旋、返回
- 蜜蜂：循环飞行动画
- 杰洛特剪影：窗内静止、搅锅动作
- 叶奈法：结局登场、行走、停驻、与白鸦同框

### 4.4 粒子与环境

需要独立粒子资源或小型 sprite sheet：

- 薄雾
- 金色光点
- 丁香花瓣
- 萤火虫
- 炊烟
- 传送门光粒子

## 5. 交付规范

### 5.1 文件格式

- 源文件：`PSD / PSB / AI / Clip Studio / Figma` 任一分层源文件
- 导出文件：`PNG`，背景透明
- 动画文件：优先 sprite sheet，其次逐帧 PNG

### 5.2 命名建议

- `scene_base.png`
- `scene_day01_courtyard_diff.png`
- `char_raven_idle_01.png`
- `particle_petals_loop.png`
- `overlay_evening_tint.png`

### 5.3 元数据要求

每个资源都建议附带：

- `asset_id`
- `day`
- `state`
- `size`
- `anchor_x`
- `anchor_y`
- `loop`
- `duration`

### 5.4 替换约束

后期从占位符替换成正式资源时，建议以下内容保持不变：

- `asset_id`
- 热点逻辑名
- 锚点位置
- 分层顺序
- 动画类型
- 触发时机

只要这些不变，替换时通常只需要改渲染资源，不需要改交互逻辑。

## 6. 优先级

### P0

- 基础场景底图
- 9 天差分
- 白鸦
- 萝卜
- 蝴蝶
- 炊烟
- 薄雾

### P1

- 杰洛特剪影
- 蜜蜂
- 萤火虫
- 紫丁香花瓣
- 结局传送门粒子

### P2

- 更细的日夜版本
- 额外环境装饰
- 更丰富的点击反馈动画

## 7. 验收标准

- 680×160 视窗下清晰可读，不糊、不挤、不串层
- 9 天变化一眼能看懂，和文案一致
- 可点击角色有静止、hover、点击反馈三个状态
- 粒子与角色动画能在同一场景带里同时运行
- 所有资源能按层复用，不需要每天整张重画

## 8. 占位符阶段验收

在正式美术到位前，占位符版本也应该满足以下标准：

- 9 天的场景变化顺序正确
- 每个可点击对象都有可见占位符
- 点击热点的范围与最终预期位置接近
- 动画节奏、切换停顿、层级关系已经稳定
- 后续替换素材时，不需要调整主要交互代码

## 9. 建议你直接交给美术的说明

请按“**一套基础全景 + 9 天差分层 + 角色/粒子小动画**”来产出，不要只交单张静态大图。  
如果项目先走占位符版本，请美术后续按既定资源位替换，不要改变热点位置、分层逻辑和基础构图。  
如果预算有限，先交 P0；如果要做高表现版本，再补 P1/P2。

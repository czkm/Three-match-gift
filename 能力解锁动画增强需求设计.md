# 能力解锁动画 + 葡萄园场景隐藏 — 需求设计

> 三个改动方向：① 能力解锁动画更精彩 ② 底部葡萄园场景优化 ③ 未解锁天数隐藏增加惊喜感。
> 全部用 CSS 驱动，不引入 Canvas 或额外依赖。

---

## 一、现状问题

### 问题 1：解锁动画缺少爆发感

当前流程：motif 动画（2-4s）→ banner 淡入（720ms，印章 + 能力药丸一起出现）→ 独白 → 按钮。

能力解锁和建筑修复文本挤在同一个 banner 里，没有独立的高光时刻。药丸（emoji 16px + 名字）视觉冲击力弱。

### 问题 2：葡萄园场景一眼看完全貌

底部 `EstateStrip` 的 `segment-grid` 始终渲染 9 个格子（`grid-template-columns: repeat(9, 1fr)`）。
未解锁的格子只是 CSS `filter: saturate(0.24) brightness(0.8)` 变灰，但轮廓、建筑结构全部可见。
玩家第一次看到就能数出 9 个区域，毫无悬念。

### 问题 3：解锁新区域没有仪式感

新建筑只是从灰变亮（`filter` 切换），附带一个 1.7s 的 `restore-shine` 光晕，
在场景边缘不太显眼。缺少"新地图展开"的惊喜。

---

## 二、设计目标

| 维度 | 目标 |
|:-----|:-----|
| 惊喜感 | 能力解锁有独立爆发瞬间；新区域像"揭开帷幕"一样出现 |
| 隐藏性 | 未解锁的天数在场景中不可见，玩家不知道后面还有多少 |
| 时长 | 解锁仪式（motif 之后）增加 4-5 秒的爆发展示 |
| 纯 CSS | 所有动画用 CSS keyframes + transition，不新增 JS 动画库 |

---

## 三、能力解锁动画增强

### 改动前时间线

```
Motif (2-4s) → Banner淡入(720ms) → 独白(3s) → 按钮
               ↑ 能力在这里，很弱
```

### 改动后时间线

```
Motif (2-4s)
    ↓
[呼吸间隔 300ms]
    ↓
★ 能力解锁爆发 (3500ms) ← 新增，独立阶段
    ↓
Banner 淡入 (720ms，只含建筑文案)
    ↓
独白 → 按钮
```

### 爆发阶段分三拍

#### 拍 1：预兆（800ms）

- 屏幕中央出现一个微小光点（8px 圆，金色径向渐变）
- 光点缓慢脉动 2-3 次（scale 0.8 → 1.2 → 0.8）
- 背景花瓣减慢（animation-duration 拉长），制造"时间凝固"感
- 整个 overlay 微微缩放（scale 0.995），屏息效果

```css
.premonition-dot {
  width: 8px; height: 8px;
  border-radius: 50%;
  background: radial-gradient(circle, var(--gold-soft) 0%, transparent 70%);
  box-shadow: 0 0 20px var(--gold-soft);
  animation: premonition-pulse 800ms ease-in-out;
}
@keyframes premonition-pulse {
  0%   { transform: scale(0.3); opacity: 0; }
  30%  { opacity: 1; }
  50%  { transform: scale(1.4); }
  70%  { transform: scale(0.8); }
  100% { transform: scale(1.1); opacity: 1; }
}
```

#### 拍 2：爆发（1500ms）— 核心

- 光点急速膨胀为冲击波圆环（从 8px → 300px），向外扩散后消失
- 冲击波之后，能力 emoji 从中心弹出（0 → 80px）
  - 弹性曲线 `cubic-bezier(0.18, 0.9, 0.34, 1.6)`
  - 带微旋转（-12deg → 0）
- emoji 两侧散射 16-20 个粒子（✨ 🪶 🌸 等，每日不同）
  - 粒子从中心向四周飞散，2 秒后消失
- 能力名称（28px 粗体）从下方滑入（translateY 20px → 0）
- 背景闪过一道暖白（opacity 0 → 0.12 → 0，200ms）

```css
.shockwave-ring {
  position: absolute;
  border-radius: 50%;
  border: 3px solid var(--gold-soft);
  box-shadow: 0 0 30px var(--gold-soft), inset 0 0 20px rgba(212, 168, 87, 0.3);
  animation: shockwave-expand 1200ms cubic-bezier(0.22, 0.9, 0.36, 1) forwards;
}
@keyframes shockwave-expand {
  0%   { width: 8px; height: 8px; opacity: 1; }
  60%  { opacity: 0.6; }
  100% { width: 320px; height: 320px; opacity: 0; }
}

.ability-hero-icon {
  font-size: 0;
  animation: hero-burst 1000ms cubic-bezier(0.18, 0.9, 0.34, 1.6) 300ms forwards;
}
@keyframes hero-burst {
  0%   { font-size: 0; transform: rotate(-12deg) scale(0); opacity: 0; }
  50%  { font-size: 84px; transform: rotate(3deg) scale(1.15); opacity: 1; }
  75%  { transform: rotate(-1deg) scale(0.95); }
  100% { font-size: 72px; transform: rotate(0) scale(1); opacity: 1; }
}

.ability-hero-name {
  opacity: 0;
  transform: translateY(18px);
  animation: name-slide-up 600ms ease-out 800ms forwards;
}
@keyframes name-slide-up {
  to { opacity: 1; transform: translateY(0); }
}

/* 粒子散射 — 每个粒子用 CSS 变量控制方向 */
.unlock-particle {
  position: absolute;
  opacity: 0;
  animation: particle-scatter 1800ms ease-out forwards;
  animation-delay: var(--pd);
}
@keyframes particle-scatter {
  0%   { transform: translate(0, 0) scale(1); opacity: 0; }
  15%  { opacity: 1; }
  100% { transform: translate(var(--px), var(--py)) scale(0.4) rotate(var(--pr)); opacity: 0; }
}
```

#### 拍 3：稳定（1200ms）

- 图标缩小到 48px，居中稳定
- 图标外围出现持续脉动的光环（透明圆环 scale 1 → 1.3 → 1 循环）
- 能力描述（`desc`，14px）淡入在图标下方
- "解锁新能力"标签从右上角飞入

```css
.ability-aura {
  position: absolute;
  width: 80px; height: 80px;
  border-radius: 50%;
  border: 2px solid var(--gold-soft);
  opacity: 0;
  animation: aura-breathe 2s ease-in-out 1400ms infinite;
}
@keyframes aura-breathe {
  0%, 100% { transform: scale(1); opacity: 0.4; }
  50%      { transform: scale(1.25); opacity: 0.15; }
}
```

### 每日粒子主题

粒子 emoji 由每日的 motif 主题决定，传递到爆发组件：

| 天 | 粒子 | 主色调 |
|:--:|:-----|:-------|
| 1 | 🪶 ✨ 💫 | 暖金 |
| 2 | 🍇 🌿 ✨ | 翠绿 |
| 3 | 🪵 ✨ 🍷 | 深棕 |
| 4 | 🐴 💨 ✨ | 土黄 |
| 5 | 🪻 🌸 ✨ | 紫丁香 |
| 6 | 🌱 💧 ✨ | 暖绿 |
| 7 | 🌅 ✨ 💫 | 暮橙 |
| 8 | 🍲 🔥 ✨ | 炉火 |
| 9 | 🪻 ⭐ ✨ | 紫银 |

---

## 四、葡萄园场景：隐藏未解锁区域

### 改动前

```
segment-grid 始终 = 9 列
[庭院][葡萄园][酒窖][马厩][花园][温室][露台][厨房][客房]
  ✓      灰      灰    灰    灰    灰    灰    灰    灰
```

所有 9 个建筑轮廓始终可见，只是灰度不同。

### 改动后

```
Day 1 开始时：
[庭院][?][?][?][?][?][?][?][?]

Day 1 完成后：
[庭院 ✓][葡萄园][?][?][?][?][?][?][?]

Day 2 完成后：
[庭院 ✓][葡萄园 ✓][酒窖][?][?][?][?][?][?]
```

- **已解锁**：正常渲染 + 全彩 + 交互热点
- **当天**：半渲染（desaturated 但有建筑轮廓），是玩家当前目标
- **未来**：完全隐藏（`display: none` 或 `width: 0`），只显示占位符

### 实现方案

#### 方案：动态列数 + 隐藏格子

将 `segment-grid` 的列数从固定 9 改为动态：

```css
.segment-grid {
  display: grid;
  /* 由 JS 绑定 style -->
  grid-template-columns: repeat(var(--visible-cols), 1fr); */
  transition: grid-template-columns 800ms cubic-bezier(0.22, 0.95, 0.34, 1);
}
```

模板改动：

```vue
<div class="segment-grid" :style="{ '--visible-cols': visibleSegments.length }">
  <article
    v-for="segment in visibleSegments"
    :key="segment.id"
    class="segment"
    :class="[segment.id, slotClass(segment), {
      'just-revealed': segment.justRevealed
    }]"
  >
    <!-- 现有内容 -->
  </article>
</div>
```

`visibleSegments` 计算属性：

```javascript
const visibleSegments = computed(() => {
  // 显示已完成的 + 当天的，隐藏未来天数
  return segments.filter(s => s.unlockCount <= displayStage.value + 1)
    .map(s => ({
      ...s,
      justRevealed: s.unlockCount === displayStage.value && revealActive.value
    }));
});
```

#### 新区域揭示动画

当某天完成修复后，对应 segment 以"从右侧展开"的方式出现：

```css
.segment.just-revealed {
  animation: segment-reveal 1200ms cubic-bezier(0.22, 0.95, 0.34, 1) forwards;
}

@keyframes segment-reveal {
  0%   {
    clip-path: inset(0 100% 0 0);   /* 整个格子从右侧被裁切 */
    opacity: 0;
    filter: saturate(0) brightness(0.6);
  }
  40%  {
    clip-path: inset(0 20% 0 0);    /* 逐步展开 */
    opacity: 1;
    filter: saturate(0.5) brightness(0.9);
  }
  100% {
    clip-path: inset(0 0 0 0);      /* 完全可见 */
    opacity: 1;
    filter: saturate(1) brightness(1);
  }
}
```

配合一道"扫光"效果：

```css
.segment.just-revealed::before {
  content: "";
  position: absolute;
  inset: 0;
  background: linear-gradient(
    90deg,
    transparent 0%,
    rgba(255, 234, 170, 0.7) 40%,
    rgba(255, 234, 170, 0.0) 60%,
    transparent 100%
  );
  z-index: 10;
  animation: segment-sweep 1000ms ease-out 200ms forwards;
  pointer-events: none;
}

@keyframes segment-sweep {
  0%   { transform: translateX(-100%); }
  100% { transform: translateX(200%); }
}
```

#### 当天未完成的格子样式

"当天"格子比已解锁的暗一些，但比完全灰的有存在感——
用一个淡淡的虚线边框暗示"这里有个地方等你修"：

```css
.segment.current-target {
  filter: saturate(0.35) brightness(0.85);
}

.segment.current-target::after {
  content: "";
  position: absolute;
  inset: 2px;
  border: 1.5px dashed rgba(208, 168, 87, 0.35);
  border-radius: 6px;
  pointer-events: none;
}
```

---

## 五、场景整体跟随天数演进

随着天数推进，不只是单个格子变亮，整个场景氛围也递进：

| 阶段 | 可见格子数 | 天空 | 地面 | 氛围 |
|:-----|:----------:|:-----|:-----|:-----|
| Day 1-2 | 2-3 | 晨光，薄雾 | 浅绿 | 安静、空旷 |
| Day 3-4 | 4-5 | 正午，阳光 | 中绿 | 温暖、生机 |
| Day 5-6 | 6-7 | 午后，丁香色 | 深绿 | 丰盛、花香 |
| Day 7-8 | 8-9 | 黄昏，暖橙 | 暮色 | 沉静、等待 |
| Day 9 | 9（全满） | 暮紫 | 深色 | 完整、归家 |

CSS transition 让天空/地面色调平滑过渡，配合 `timeClass` 切换。

---

## 六、能力爆发与场景揭示的衔接

当 `PerDayCutscene` 的爆发阶段结束、banner 淡入时，
底部 `EstateStrip` 同步触发新 segment 的揭示动画：

1. 爆发阶段中（Phase C），EstateStrip 保持当前状态
2. banner 淡入的瞬间，EstateStrip 的 `revealActive` 被触发
3. 新 segment 用 `clip-path` 展开 + 扫光
4. 整个场景的 `timeClass` 平滑过渡到下一个色调

时间线：
```
爆发 Phase C 结束
    ↓ (同时)
Banner 淡入 + EstateStrip 新区域展开
    ↓
独白
```

---

## 七、涉及文件

| 文件 | 改动 | 说明 |
|:-----|:-----|:-----|
| `PerDayCutscene.vue` | **重构** | 拆出爆发阶段，新增拍 1/2/3 |
| `cutscenes/AbilityBurst.vue` | **新建** | 能力爆发动画组件 |
| `EstateStrip.vue` | **重构** | 动态列数、隐藏未解锁、揭示动画 |
| `animations.css` | **扩展** | 冲击波、粒子散射、光环脉动、扫光 |
| `timing.js` | **扩展** | 新增爆发阶段时序常量 |
| `content.js` | **小改** | 每日粒子主题配置 |

不改动的文件：`gameStore.js`（状态管理不变）、`AbilityBar.vue`（无引导改动）、`GameContainer.vue`（流程衔接不变）。

---

## 八、CSS 性能守则

- 所有移动动画用 `transform` + `opacity`，不用 `width/height/left/top`
- 冲击波用 `scale()` 而非尺寸动画
- 粒子用 `will-change: transform, opacity` 一次性声明
- `clip-path: inset()` 动画比 `clip-path: polygon()` 性能好
- 粒子上限 20 个，动画结束后 `v-if` 移除 DOM
- `segment-grid` 的 `grid-template-columns` transition 由浏览器合成层处理

---

## 九、Day 9 特殊处理

- 冲击波更大（`scale` 到 5 而非 4）
- 粒子数量翻倍（32 个）
- 爆发后 banner 按钮改为"迎接归来"
- 最后一个 segment 揭示时不裁切，改为整体从暗到亮渐变（暗示"旅程终点"）

---

## 十、验收标准

- [ ] 能力解锁有独立的三拍爆发动画（预兆 → 冲击波+图标弹出 → 稳定）
- [ ] 爆发阶段不含任何文字引导，纯粹是视觉奖励
- [ ] 未解锁天数在葡萄园场景中完全不可见
- [ ] 新区域展开用 clip-path + 扫光动画
- [ ] 当天目标格子有虚线边框提示
- [ ] 9 天的粒子主题/颜色各不相同
- [ ] 天空/地面色调随天数递进
- [ ] 全部动画用 CSS 驱动，无新增 JS 依赖
